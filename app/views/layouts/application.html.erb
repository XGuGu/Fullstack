<!DOCTYPE html>
<html>

  <head>
    <title>Pindup</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <script>
     window.logo = "<%= asset_path('logo.png')%>"
     window.new_board = "<%= asset_path('new_board') %>"
     window.create_board ="<%= asset_path('create_board.png') %>"
    </script>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
